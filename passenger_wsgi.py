from run import app as application
# This file is used by the WSGI server to run the Flask application.

# The application object is named 'application' to be compatible with WSGI standards.
# The 'application' variable is the entry point for the WSGI server to run the Flask app.