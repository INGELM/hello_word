---
deployment:
      tasks:
        - export DEPLOYPATH=/home2/globalstweb/hello_world  # Add /<app_folder> if required
        - /bin/cp -r * $DEPLOYPATH                        #copy all recursively to $DEPLOYPATH
        
        
